import React from 'react'

function Coin() {

    // 1.Fetch info about a single coin (by using either coin name or id, picking it up from the url with useParams hook)
    //2. fetch the info for the chart using coin's name
  return (
      <div>
        
          <h2>additional info about an specific coin</h2>
    </div>
  )
}

export default Coin